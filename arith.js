// Perform calculations and assign results to variables
let add = 2 + 2; // Addition
let sub = 2 - 2; // Subtraction
let mul = 2 * 2; // Multiplication
let div = 2 / 2; // Division
let mod = 2 % 2; // Modulus

// Output the results
console.log(`add = ${add}`);
console.log(`sub = ${sub}`);
console.log(`mul = ${mul}`);
console.log(`div = ${div}`);
console.log(`mod = ${mod}`);